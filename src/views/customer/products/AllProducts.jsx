const React = require('react');
const Layout = require('../../common/Layout');

module.exports = function Register({ title }) {
  return (
    <Layout title={title}>
      <h1>Все товары</h1>
      <p>Список товаров...</p>
    </Layout>
  );
};
